今天主管不在，所以只能自己摸索，看了一点微软开源的Azure IOT Edge，发现其实边缘节点就是在边缘设备上安装的Docker容器，其中最基本的镜像就是agent负责pull和部署，hub负责往云端push。此外，总结一下github流程，先是git add，然后git commit -m "",然后是git remote add origin "url",然后是git push origin master,有时可能会push失败，显示需要先pull，则操作git pull --rebase origin master.此外go写mqtt协议的流程就是先有clientOps对象，通过clientOptions := mqtt.NewClientOptions获取  《//这里面设置连接的方式和一些参数以便启动》，听过mqtt.NewClient(clientOptions)，此方法会以传递进去的clientOptions方式来创建并初始化client，