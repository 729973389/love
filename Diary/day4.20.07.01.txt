由于昨天在go test 环节，导入的包一直报错，整个上午都卡了。无意中发现有pkg包，里面有module和modsum文件，这为之后埋下了伏笔，幸好远敏蛮照顾我的，过来帮我解决了很多问题，首先我的项目结构太乱，导致它在多个gopath路径下寻找错误，而且远敏提供了一个全新的概念，go module要比gopath好用很多，之后gomodule又卡住了，原来go module项目，项目是直接和*.mod是并行关系，不需要src和bin文件了，而且*.mod是不能放在path下的，而且在gopath下也不能出现多个pkg包，总结起来，创建gomodule项目流程如下，创建 go module项目，在 environment中添加proxy，value为 https://goproxy.go，《注意路径一定要写名”scheme://url:port“ 的形式》，然后就在项目下直接新建.go文件，由于我写的是一个test,所以命名格式要如下，一个xxx_test,一个功能测试函数TestXxx,其中入口要导入testing包，函数的入口要以func TestXxx(t *testing.T)形式，因为这个是t提供了汇报测试失败和日志记录的功能。之后的话又和远敏探讨了一下给IOT平台发包，流程如下：	1<是建立profile物模型，因为根据物模型来写json。	2<记录下发给我们的设备ID和密钥，接下来的加密要用。	3<clientId = "deviceId_(0\1\2)_(0\1)_YYYYMMDDHH"，其中deviceId就是前面发给我们的设备ID，(0\1\2)是鉴权类别，0\2都是一机一秘，0代表deciveId接入，2代表nodeId接入，1代表一个型号一个密码。(0\1)是密码类型，0代表HMACSHA256不校验时间戳，1则代表校验时间戳，时间戳如2020/07/01/23：29：33则表示为2020070123.	4<Username根据鉴权类别来  	5<Password 为HMACSHA256算法，以时间戳为密钥，以发送给我们的密码为secret。